interface:
  display_name: "家具智能体"
  short_description: "路由六阶段家具生成与独立房间布局"
  default_prompt: "使用 furniture_* 工具逐步确认与生成，展示 current_output 后等待确认；CAD 必须 furniture_run_next(generate_cad=true)。不要调用 execute_spec 或 plan_cabinet。"
