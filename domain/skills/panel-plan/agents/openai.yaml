interface:
  display_name: "家具板件规划"
  short_description: "确认结构、净空、背板并生成实体板件"
  default_prompt: "使用 $panel-plan。只读已冻结布局的柜体外包络，提议门、层板、抽屉、背板、踢脚和料厚。没说清的构造值标成假设；料厚按工艺卡展开，不进假设清单。另一版用 retry_stage，不要重做意图。准入后展示净空和板件清单，等人确认。"
