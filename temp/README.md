# Temporary scripts

Put one-off inspection, migration, debugging, and CAD experiment scripts here.
Everything else in this directory is ignored by Git.

Reusable furniture workflow code belongs in
`skills/furniture-cad/scripts/` instead.
